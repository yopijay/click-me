// // Library
// import { render } from "@testing-library/react";
import App from "./App"; // Component

// describe(App, () => {
//     it("count should increment by 1 if the button is pressed", () => {
//         const { getByTestId, getByRole } = render(<App />);
//         const clickedmebtn = getByRole("box", { name: "Click Me!"});

//         fireEvent.click(clickedmebtn);
//         const counter = Number(getByTestId("counter").textContent);
//     })
// });

describe(App, () => {
    
});